{
  "collectionName": "components_shared_styled_texts",
  "info": {
    "displayName": "Styled Text"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "text",
      "required": true
    },
    "font_size": {
      "type": "enumeration",
      "required": true,
      "default": "Normal",
      "enum": [
        "Small",
        "Normal",
        "Large",
        "Extra-Large"
      ]
    },
    "color": {
      "type": "enumeration",
      "required": true,
      "default": "Default",
      "enum": [
        "Default",
        "Website-Theme-Color1",
        "Website-Theme-Color2"
      ]
    },
    "font_style": {
      "type": "enumeration",
      "required": true,
      "default": "Normal",
      "enum": [
        "Normal",
        "Italic",
        "Bold",
        "Underline"
      ]
    }
  },
  "config": {}
}
